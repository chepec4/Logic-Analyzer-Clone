// ============================================================================
// LEGACY DIALOGS (Reference Only - UI is dynamic in C4 Architecture)
// ============================================================================

#include "resource.h"
#include <windows.h> // [C4 FIX] Reemplazo de afxres.h (MFC) para MinGW

// Macros de compatibilidad si faltan
#ifndef IDC_STATIC
#define IDC_STATIC -1
#endif

// ----------------------------------------------------------------------------
// Dialog Definitions
// ----------------------------------------------------------------------------

SETTINGS DIALOGEX 0, 0, 218, 256
STYLE DS_SETFONT | DS_CONTROL | WS_CHILD | WS_VISIBLE
EXSTYLE WS_EX_CONTROLPARENT
FONT 8, "MS Shell Dlg 2", 0, 0, 0x0
BEGIN
    // Definiciones legacy...
    // Estos controles no son usados por sa.editor.h, que crea widgets C++
    COMBOBOX        0,64,8,48,84,CBS_DROPDOWNLIST | WS_VSCROLL | WS_TABSTOP
    LTEXT           "Channel",IDC_STATIC,8,10,42,8
    // ...
END

// ----------------------------------------------------------------------------
// Bitmaps
// ----------------------------------------------------------------------------
// TOOLBAR-ON  BITMAP  "toolbar-on.bmp"
// TOOLBAR-OFF BITMAP  "toolbar-off.bmp"
